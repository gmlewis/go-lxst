package lxst

// VERSION is on a line by itself so that it can be bumped by the GitHub Action
// workflow "on-push-to-master.yml" and then the repo can be automatically tagged
// as the latest version.
const VERSION = "0.5.0"
